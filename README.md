# SpawnProtect
SCP: CS plugin

При спавне за какую-либо роль выдаётся временное бессмертие на 3 (по умолчанию) секунды.

Некоторые параметры можно менять через конфиг:
* `spawn_protect_time` - время, на которое  выдаётся бессмертие (по умолчанию: 3).
